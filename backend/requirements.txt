# Optional dependencies for FastAPI / Uvicorn deployment
# Note: FraudNexus includes a built-in zero-dependency HTTP server that runs natively with standard Python 3.13!
fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.6.0
sqlalchemy>=2.0.25
pytest>=8.0.0
httpx>=0.27.0
